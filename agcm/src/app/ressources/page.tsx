// src/app/ressources/page.tsx
// Section ressources supprimée — redirection vers la landing
import { redirect } from 'next/navigation';

export default function RessourcesPage() {
  redirect('/');
}
